kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-t-infra-postgres
  labels:
    app: t-infra-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi